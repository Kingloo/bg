[package]
name = "bg"
version = "1.3.0"
edition = "2018"

[dependencies]
rand = { version = "0.8.5", features = [ "std_rng" ] }

[dependencies.windows]
version = "0.33.0"
features = [
	"Win32_Foundation",
	"Win32_System_Com",
	"Win32_UI_Shell"
]

[profile.dev]
opt-level = 0

[profile.release]
opt-level = 3
lto = true
