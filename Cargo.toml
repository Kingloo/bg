[package]
name = "bg"
version = "1.1.0"
edition = "2018"

[dependencies]
rand = { version = "0.8.4", features = [ "std_rng" ] }

[dependencies.windows]
version = "0.28"
features = [
	"Win32_Foundation",
	"Win32_System_Com",
	"Win32_UI_Shell"
]

[profile.release]
lto = true
